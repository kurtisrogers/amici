{
  "name": "amici-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Browser tests for Amici. Kept in its own directory so the Go module has no Node dependency.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
